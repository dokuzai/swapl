{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd app && rm -rf node_modules && npm install",
  "buildCommand": "cd app && rm -rf .next .turbo && prisma generate --schema prisma/schema.postgres.prisma && next build --webpack",
  "outputDirectory": "app/.next",
  "crons": [
    { "path": "/api/cron/featured-expire", "schedule": "0 1 * * *" },
    { "path": "/api/cron/saved-searches", "schedule": "0 9 * * *" }
  ]
}
