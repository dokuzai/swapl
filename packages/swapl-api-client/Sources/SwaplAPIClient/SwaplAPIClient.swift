// The API types + `Client` are generated at build time by swift-openapi-generator
// from openapi.yaml (see openapi-generator-config.yaml). This file keeps the
// target non-empty and will host client conveniences (e.g. a bearer middleware).

public enum SwaplAPIClientInfo {
    public static let generatedFromOpenAPI = true
}
